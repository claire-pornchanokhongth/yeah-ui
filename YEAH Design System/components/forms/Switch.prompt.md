Use for settings that apply instantly. For form submissions use Checkbox instead.

```jsx
<Switch label="Email me about events" defaultChecked onChange={v => save(v)} />
```

48×28 track, pill, pink when on. `onChange` gives you the boolean directly.

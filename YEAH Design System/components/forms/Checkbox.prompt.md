Multi-select control. 22px box, 4px radius, pink fill + white Lucide check.

```jsx
<Checkbox label="ยอมรับเงื่อนไข" defaultChecked />
```

Stack groups with flex + `gap: var(--space-3)`. Works uncontrolled out of the box.

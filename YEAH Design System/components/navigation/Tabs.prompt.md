Section switcher. Underline variant sits on a 2px black rule with a 3px pink active marker.

```jsx
<Tabs items={['Events','Members','Resources']} onChange={setView} />
```

Use `variant="pill"` for filters inside a Card, never for top-level page navigation.

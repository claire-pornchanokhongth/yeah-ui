Single choice among 2–4 visible options.

```jsx
<Radio name="plan" label="Member" defaultChecked />
<Radio name="plan" label="Mentor" />
```

The inner dot scales in over 140ms — the only "grow" animation in the system.

Short-lived feedback. Stack bottom-right with `gap: var(--space-3)`.

```jsx
<Toast tone="success" title="You're in" message="Check your email for the QR code." onDismiss={hide} />
```

`info` is black (not blue) — the system has no blue. Keep messages to one line.
